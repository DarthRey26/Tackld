import ReactDOM from "react-dom/client";
import App from "./App"; // Correct the import path based on your folder structure
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
